# 绘画分析系统时间限制更新文档

## 概述
根据需求，AI对话系统现在只参考用户当日或4小时内的最新绘画分析内容。如果最新分析内容不在这个时间范围内，则不参考任何分析结果。

## 重要改进
- **实时更新**：AI在每次对话前都会重新获取最新的绘画分析，确保不会丢失更新
- **用户隔离**：每个用户都有独立的上下文管理，避免用户间数据混乱
- **详细日志**：完整的分析获取和使用过程日志记录

## 更新内容

### 1. 数据库层改进

#### 新增方法：`get_recent_analysis(user_id, hours)`
- **功能**：获取用户在指定小时内的最新绘画分析结果
- **参数**：
  - `user_id`: 用户ID
  - `hours`: 时间范围（小时），0表示当日
- **返回值**：分析结果字典或None

**时间逻辑**：
- `hours=0`：获取当日（从00:00开始到23:59:59）的分析结果
- `hours=4`：获取最近4小时内的分析结果
- 其他数值：获取对应小时数内的分析结果

### 2. AI对话系统改进

#### 实时分析获取机制
- **每次对话触发**：用户每发送一条消息，系统都会重新从数据库获取最新分析
- **防止数据丢失**：确保即使在对话过程中用户完成了新的绘画分析，AI也能立即感知并使用

#### 分析参考逻辑
1. **优先级1**：首先尝试获取当日的分析结果
2. **优先级2**：如果当日没有，获取4小时内的分析结果
3. **兜底策略**：如果都没有，则不参考任何分析结果

#### 系统消息构建
- **有符合条件的分析**：系统消息包含分析结果和日期
- **无符合条件的分析**：使用标准系统消息，不包含分析内容

#### 用户上下文管理
- **独立上下文**：每个用户都有独立的对话上下文存储
- **避免混乱**：解决了之前全局变量导致的多用户数据混乱问题

### 3. API接口改进

#### `/api/user/latest-analysis` 接口增强
新增查询参数 `time_limit`：

- `none`（默认）：获取最新分析结果（不限时间）
- `today`：获取当日分析结果
- `4hours`：获取4小时内分析结果
- `recent`：获取当日或4小时内分析结果（优先当日）

**使用示例**：
```
GET /api/user/latest-analysis?time_limit=recent
GET /api/user/latest-analysis?time_limit=today
GET /api/user/latest-analysis?time_limit=4hours
```

## 技术实现细节

### 时间计算逻辑
```python
# 当日分析：从当天00:00到23:59:59
start_of_day = current_time.replace(hour=0, minute=0, second=0, microsecond=0)
end_of_day = start_of_day + datetime.timedelta(days=1)

# N小时内分析：从当前时间往前推N小时
time_threshold = current_time - datetime.timedelta(hours=hours)
```

### 数据库查询优化
- 使用复合索引：`(user_id, created_at, is_active)`
- 查询条件结合时间范围和用户ID
- 按创建时间降序排列，获取最新记录

## 应用场景

### 场景1：用户刚完成绘画分析
- 分析时间：今天14:30
- 用户对话时间：今天15:00
- **结果**：AI参考当日分析结果

### 场景2：用户在4小时内完成分析
- 分析时间：今天10:00
- 用户对话时间：今天13:30
- **结果**：AI参考4小时内分析结果

### 场景3：用户的分析超过4小时且不是当日
- 分析时间：昨天20:00
- 用户对话时间：今天10:00
- **结果**：AI不参考任何分析结果

### 场景4：用户有当日分析，也有4小时内的其他分析
- 当日分析：今天08:00
- 4小时内分析：今天14:00
- 用户对话时间：今天15:00
- **结果**：优先参考当日分析（08:00的分析）

## 日志记录
系统会记录以下详细信息：

### 分析获取日志
- **成功获取**：记录分析ID、用户ID、分析日期、创建时间
  ```
  ✅ 成功获取用户 {user_id} 的分析结果 - ID: {analysis_id}, 日期: {analysis_date}, 创建时间: {analysis_time}
  ```
- **未找到分析**：记录用户ID和时间限制条件
  ```
  ℹ️  用户 {user_id} 在当日或4小时内暂无分析记录
  ```
- **查询错误**：记录错误信息和用户ID
  ```
  ❌ 获取用户 {user_id} 的分析结果失败: {error_message}
  ```

### AI对话日志
- **分析参考确认**：记录AI使用的分析来源
  ```
  🎯 AI将基于 {analysis_date} 的分析结果进行对话
  ```
- **无分析参考**：记录AI不使用分析的情况
  ```
  🔄 AI将不参考任何分析结果进行对话（无符合时间条件的分析）
  ```

### 实时更新日志
- **分析获取开始**：每次对话开始时记录
  ```
  开始获取用户 {user_id} 的最新分析结果...
  ```

## 兼容性说明
- 保留原有的 `get_latest_analysis()` 方法，确保向后兼容
- API接口保持向后兼容，默认行为不变
- 新功能为可选功能，不影响现有流程

## 测试建议
1. 测试当日分析参考功能
2. 测试4小时内分析参考功能
3. 测试超时分析不参考功能
4. 测试API接口的各种时间限制参数
5. 测试边界情况（恰好4小时、跨日期等）
